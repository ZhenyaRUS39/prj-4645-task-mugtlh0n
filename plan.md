# Plan: prj-4645-task-mugtlh0n

**Project:** `prj-4645-task-mugtlh0n`  
**Task ID:** `task-mugtlh0n`  
**Repo:** `prj-4645-task-mugtlh0n`  

## Summary

План: инициализировать Express v2 проект (ESM, структура каталогов), реализовать CRUD заметок с in-memory storage и валидацией, написать unit-тесты и финальный smoke-тест через supertest.

## Subtasks

### 1. Инициализация проекта Express v2

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать структуру репозитория: package.json (type=module), директории src/, src/routes/, src/storage/, src/middleware/, tests/. Установить express@5 (v2-ветка), nodemon для dev, vitest и supertest для тестов. Добавить скрипты start, dev, test.
- **Test plan:** npm install завершается без ошибок; package.json содержит express ^5.0.0; node --check src/app.js (или main) проходит; директории src/routes, src/storage, tests существуют.

### 2. Реализация in-memory хранилища заметок

- **ID:** `sub-2`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать src/storage/notesStore.js: Map с методами list, get(id), create({title, content}), update(id, patch), delete(id). Автоинкремент id. Возврат копий объектов (не ссылок) чтобы избежать мутаций.
- **Test plan:** Импорт в node-скрипте: create -> id=1, list -> [{id:1}], get(1) -> объект, update(1, {title:'x'}) -> объект с новым title, delete(1) -> true, get(1) -> undefined.
- **Dependencies:** `sub-1`

### 3. Реализация CRUD роутов и middleware валидации

- **ID:** `sub-3`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать src/middleware/validate.js (проверка title/content обязательны) и src/routes/notes.js: GET /notes, GET /notes/:id, POST /notes, PUT /notes/:id, DELETE /notes/:id. Корректные HTTP-коды: 200/201/204/400/404. Смонтировать в src/app.js, экспортировать app для тестов. Запустить сервер только если require.main === module.
- **Test plan:** Ручной smoke: POST без title -> 400, POST валидный -> 201, GET /notes -> массив, GET /notes/999 -> 404, PUT /notes/1 -> 200, DELETE /notes/1 -> 204. app.js экспортируется как default export.
- **Dependencies:** `sub-2`

### 4. Unit + integration тесты (vitest + supertest)

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Покрыть тестами tests/notesStore.test.js (store) и tests/notesApi.test.js (полный CRUD через supertest). Минимум 8 кейсов: list empty, create+get, update, delete, 404 на missing, 400 на пустом title, 400 на пустом content, DELETE несуществующего -> 404.
- **Test plan:** npm test -> все тесты зелёные, минимум 8 ассертов проходят, покрытие store и роутов полное. Никаких flaky на параллельных запусках.
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
